# collections

Array operations for Mozhi: sort, reverse, sum, min, max, mean, slice, concat, range.

## Usage
```mozhi
import mod from "collections"
nums = [3, 1, 4, 1, 5, 9, 2, 6]
echo(mod.sort(nums))
echo(mod.sum(nums))
echo(mod.max(nums))
echo(mod.range(0, 10))
```
